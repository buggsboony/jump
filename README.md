# jump directly to a directory, like cd $directory
# example (from ~/):
jump downloads
# example specifying parent directory (from ~/):
jump parent/downloads
# example specifying part of parent directory name (from ~/):
jump *rent/downloads
